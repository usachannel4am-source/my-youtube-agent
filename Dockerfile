FROM n8nio slash n8n colon latest
USER root
RUN apk update
RUN apk add, double dash no-cache, ffmpeg, python3, py3-pip,
RUN pip install, double dash no-cache-dir, double dash break-system-packages, edge-tts
USER node
